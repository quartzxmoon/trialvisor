import { router, json, error, requireAuth, db, notifications, secrets, type RouterContext } from '@appdeploy/sdk';
import { ACTIVE_STATUSES, billingConfigured, billingSnapshot, createCheckout, createPortal, currentBilling, handleStripeWebhook, hasProEntitlement } from './billing';
import { canvaCancellationEvidence, classifySignal, providerSupport, type GmailMessage, type ProviderSupport } from './provider-signals';
import { deleteAllRecords as deleteAllPagedRecords, listAllMatching } from './paged-records';
import { SESSION_IDLE_TIMEOUT_MS, SESSION_ABSOLUTE_LIFETIME_MS, SENSITIVE_ACTION_MAX_AGE_MS, getSessionId, resolveSession as resolveSessionRecord, cleanupSessions as cleanupSessionsRecords, evaluateSession, invalidateSession as invalidateSessionRecord, type UserSession } from './session';
type State='REVIEW_REQUIRED'|'AUTO_CANCEL_ENABLED'|'DECISION_PENDING'|'KEEP_REQUESTED'|'CANCELLATION_RUNNING'|'CANCELLATION_NEEDS_USER'|'CANCELED_CONFIRMED';
type Trial={userId:string;provider:string;plan:string;price:number;currency:string;trialEnd:string;safeDeadline:string;plannedExecution:string;state:State;tier:string;support?:ProviderSupport;confidence:number;confidenceBand?:'HIGH'|'MEDIUM'|'LOW';sourceRef?:{provider:'google'|'microsoft';messageId:string;threadId:string;receivedAt?:string};verificationRef?:{provider:'google';messageId:string;threadId:string;receivedAt?:string};evidence?:string;nextAction?:string;authorizationAt?:string;providerActionAt?:string;cancellationUrl?:string};
type Job={userId:string;trialId:string;kind:'DECISION'|'CANCEL';dueAt:string;status:'PENDING'|'REVOKED'|'DONE';attempts:number;idempotencyKey:string};
type OAuthState={userId:string;provider:'google';nonceHash:string;createdAt:string;expiresAt:string;usedAt?:string};
type GoogleTokenRecord={id:string;userId:string;provider:'google';providerAccountIdentifier:string;ciphertext:string;iv:string;tokenExpiration:string;authorizedScopes:string;createdAt:string;updatedAt:string;revokedAt?:string};
type PersistentNotice={id?:string;userId:string;kind:string;title:string;message:string;severity:'info'|'warning'|'critical'|'success';at:string;readAt?:string|null;trialId?:string;provider?:string;actionView?:'overview'|'accounts'|'billing'|'settings'};
const GOOGLE_REDIRECT_URI='https://api-v2.appdeploy.ai/app/trialvisor-m8vrsu/api/oauth/google/callback';
const GOOGLE_SCOPES=['openid','email','profile','https://www.googleapis.com/auth/gmail.readonly'];
const bytesToHex=(v:Uint8Array)=>Array.from(v).map(x=>x.toString(16).padStart(2,'0')).join('');
const bytesToB64=(v:Uint8Array)=>btoa(String.fromCharCode(...v));
const randomHex=(bytes:number)=>{const v=new Uint8Array(bytes);crypto.getRandomValues(v);return bytesToHex(v)};
const sha256=async(s:string)=>bytesToHex(new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(s))));
const secretNames=async()=>new Set(await secrets.listSecretNames());
const googleReady=async()=>{const n=await secretNames();return n.has('GOOGLE_CLIENT_ID')&&n.has('GOOGLE_CLIENT_SECRET')&&n.has('GOOGLE_TOKEN_ENCRYPTION_KEY')};
const tokenKey=async(usage:KeyUsage[])=>{const raw=await secrets.readSecret('GOOGLE_TOKEN_ENCRYPTION_KEY');const keyBytes=Uint8Array.from(atob(raw),c=>c.charCodeAt(0));if(keyBytes.length!==32)throw new Error('GOOGLE_TOKEN_ENCRYPTION_KEY must be base64 for exactly 32 bytes');return crypto.subtle.importKey('raw',keyBytes,'AES-GCM',false,usage)};const encryptTokens=async(value:unknown)=>{const key=await tokenKey(['encrypt']),iv=new Uint8Array(12);crypto.getRandomValues(iv);const encrypted=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,new TextEncoder().encode(JSON.stringify(value)));return{ciphertext:bytesToB64(new Uint8Array(encrypted)),iv:bytesToB64(iv)}};const decryptTokens=async(ciphertext:string,iv:string)=>{const key=await tokenKey(['decrypt']),data=Uint8Array.from(atob(ciphertext),c=>c.charCodeAt(0)),ivBytes=Uint8Array.from(atob(iv),c=>c.charCodeAt(0));const plain=await crypto.subtle.decrypt({name:'AES-GCM',iv:ivBytes},key,data);return JSON.parse(new TextDecoder().decode(plain)) as {access_token?:string;refresh_token?:string}};
const googleFetch=async(url:string,accessToken:string)=>fetch(url,{headers:{Authorization:'Bearer '+accessToken}});
const destroyGoogleToken=async(u:string,x:GoogleTokenRecord,reason:string)=>{const now=new Date().toISOString();await db.update(table(u,'google-tokens'),[{id:x.id,record:{...x,ciphertext:'',iv:'',revokedAt:now,updatedAt:now}}]);await audit(u,'connection','GOOGLE_REAUTH_REQUIRED',reason)};
const googleAccessToken=async(u:string,x:GoogleTokenRecord)=>{const plain=await decryptTokens(x.ciphertext,x.iv) as {access_token?:string;refresh_token?:string;expires_in?:number};if(new Date(x.tokenExpiration).getTime()>Date.now()+60000&&plain.access_token)return plain.access_token;if(!plain.refresh_token){await destroyGoogleToken(u,x,'Google access expired without a refresh token.');throw new Error('reconnect_required')}const clientId=await secrets.readSecret('GOOGLE_CLIENT_ID'),clientSecret=await secrets.readSecret('GOOGLE_CLIENT_SECRET');const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:clientId,client_secret:clientSecret,refresh_token:plain.refresh_token,grant_type:'refresh_token'})});if(!r.ok){await destroyGoogleToken(u,x,'Google refresh was rejected; local token material destroyed.');throw new Error('reconnect_required')}const fresh=await r.json() as {access_token:string;expires_in:number;refresh_token?:string};const merged={...plain,...fresh,refresh_token:fresh.refresh_token||plain.refresh_token},encrypted=await encryptTokens(merged),now=new Date().toISOString();await db.update(table(u,'google-tokens'),[{id:x.id,record:{...x,...encrypted,tokenExpiration:new Date(Date.now()+fresh.expires_in*1000).toISOString(),updatedAt:now}}]);await audit(u,'connection','GOOGLE_TOKEN_REFRESHED','Google access refreshed server-side.');return fresh.access_token};
const GMAIL_PROCESSOR_VERSION=4;
const GMAIL_CANDIDATE_CAP=180;
const normalizedPlan=(value:string)=>value.toLowerCase().replace(/\b(?:your|free|trial|has|started|ends?|ending|expires?|renewal|subscription|membership|confirmation|reminder|will|be|charged|after|on)\b/g,' ').replace(/[^a-z0-9]+/g,' ').trim();
const syncGoogle=async(u:string)=>{
 const records=(await db.list<GoogleTokenRecord>(table(u,'google-tokens'),{limit:10})).items.filter(x=>!x.revokedAt&&x.ciphertext);
 if(!records.length)throw new Error('google_not_connected');
 let scanned=0,created=0,duplicates=0,fetchFailures=0,matched=0,verifiedCancellations=0,high=0,medium=0,low=0,incrementalCandidates=0,searchCandidates=0,capped=false;
 const modes=new Set<string>();
 for(const rec of records){
  const access=await googleAccessToken(u,rec),profileRes=await googleFetch('https://gmail.googleapis.com/gmail/v1/users/me/profile',access);
  if(!profileRes.ok)throw new Error('gmail_profile_failed_'+profileRes.status);
  const profile=await profileRes.json() as {historyId:string};
  const checkpoints=(await db.list<any>(table(u,'google-sync'),{limit:10})).items,checkpoint=checkpoints.find(x=>x.providerAccountIdentifier===rec.providerAccountIdentifier);
  const seen=(await db.list<any>(table(u,'gmail-sources'),{limit:500})).items,seenById=new Map(seen.map(x=>[x.messageId,x])),hasMatched=seen.some(x=>x.matched);
  const ids=new Set<string>();let needsFullSearch=!checkpoint?.historyId;
  if(checkpoint?.historyId){
   let pageToken='';
   for(let page=0;page<3&&ids.size<GMAIL_CANDIDATE_CAP;page++){
    const historyUrl='https://gmail.googleapis.com/gmail/v1/users/me/history?startHistoryId='+encodeURIComponent(checkpoint.historyId)+'&historyTypes=messageAdded&maxResults=100'+(pageToken?'&pageToken='+encodeURIComponent(pageToken):'');
    const historyResponse=await googleFetch(historyUrl,access);
    if(historyResponse.status===404){checkpoint.historyId='';needsFullSearch=true;modes.add('checkpoint_fallback');break}
    if(historyResponse.status===401){await destroyGoogleToken(u,rec,'Google rejected the access token.');throw new Error('reconnect_required')}
    if(!historyResponse.ok)throw new Error('gmail_history_failed_'+historyResponse.status);
    const data=await historyResponse.json() as {history?:Array<{messagesAdded?:Array<{message:{id:string}}>}>;nextPageToken?:string};
     for(const event of data.history||[])for(const added of event.messagesAdded||[]){if(ids.size<GMAIL_CANDIDATE_CAP)ids.add(added.message.id);else capped=true}
     pageToken=data.nextPageToken||'';if(!pageToken)break;if(page===2)capped=true;
   }
   if(ids.size){incrementalCandidates+=ids.size;modes.add('incremental')}
  }
  if(needsFullSearch||!hasMatched){
   modes.add(needsFullSearch?'initial_search':'validation_search');
   const queries=[
    'newer_than:2y {subject:"free trial" subject:"trial ends" subject:"trial ending" subject:"trial expires" "after your trial" "will be charged"}',
    'newer_than:1y {"subscription renewal" "membership renews" "renews on" "next billing date" "upcoming payment"}',
    'newer_than:1y from:(canva.com) {"cancellation confirmed" "subscription has been cancelled" "subscription has been canceled" "plan was cancelled" "plan was canceled"}'
   ];
    for(const query of queries){
     if(ids.size>=GMAIL_CANDIDATE_CAP){capped=true;break}
     let pageToken='';
     for(let page=0;page<2&&ids.size<GMAIL_CANDIDATE_CAP;page++){
      const url='https://gmail.googleapis.com/gmail/v1/users/me/messages?q='+encodeURIComponent(query)+'&maxResults=100'+(pageToken?'&pageToken='+encodeURIComponent(pageToken):'');
      const response=await googleFetch(url,access);
      if(response.status===401){await destroyGoogleToken(u,rec,'Google rejected the access token.');throw new Error('reconnect_required')}
      if(!response.ok)throw new Error('gmail_search_failed_'+response.status);
      const data=await response.json() as {messages?:Array<{id:string}>;nextPageToken?:string};
      const before=ids.size;for(const item of data.messages||[]){if(ids.size<GMAIL_CANDIDATE_CAP)ids.add(item.id);else capped=true}searchCandidates+=ids.size-before;
      pageToken=data.nextPageToken||'';if(!pageToken)break;if(page===1)capped=true;
     }
   }
  }
  const existingTrials=await listTrials(u);
   const candidateIds:string[]=[];
   for(const id of Array.from(ids).slice(0,GMAIL_CANDIDATE_CAP)){
    const prior=seenById.get(id);
    if(prior?.matched||Number(prior?.processorVersion||0)>=GMAIL_PROCESSOR_VERSION){duplicates++;continue}
    candidateIds.push(id);
   }
   const CHUNK_SIZE=6;
   for(let i=0;i<candidateIds.length;i+=CHUNK_SIZE){
    const chunk=candidateIds.slice(i,i+CHUNK_SIZE);
    const batch=await Promise.all(chunk.map(async id=>{
     try{
      const res=await googleFetch('https://gmail.googleapis.com/gmail/v1/users/me/messages/'+encodeURIComponent(id)+'?format=full',access);
      if(!res.ok)return{id,ok:false as const};
      const msg=await res.json() as GmailMessage;
      return{id,ok:true as const,message:msg};
     }catch{
      return{id,ok:false as const};
     }
    }));
    for(const item of batch){
     scanned++;
     if(!item.ok){fetchFailures++;continue}
     const id=item.id,message=item.message,prior=seenById.get(id);
     const evidence=canvaCancellationEvidence(message);
     if(evidence){
      const eligible=existingTrials.filter(t=>/\bcanva\b/i.test(t.provider)&&(t.support||providerSupport(t.provider).support)==='GUIDED'&&['AUTO_CANCEL_ENABLED','DECISION_PENDING','CANCELLATION_RUNNING','CANCELLATION_NEEDS_USER'].includes(t.state)&&!!t.authorizationAt&&new Date(evidence.receivedAt).getTime()>=new Date(t.authorizationAt!).getTime());
      const sourceRecord={messageId:id,threadId:message.threadId,processedAt:new Date().toISOString(),matched:eligible.length===1,kind:'cancellation_confirmation',processorVersion:GMAIL_PROCESSOR_VERSION};
      if(prior)await db.update(table(u,'gmail-sources'),[{id:prior.id,record:{...prior,...sourceRecord}}]);
      else await db.add(table(u,'gmail-sources'),[sourceRecord]);
      if(eligible.length===1){
       const t=eligible[0],confirmed={...t,...providerSupport(t.provider),state:'CANCELED_CONFIRMED' as State,verificationRef:{provider:'google' as const,messageId:id,threadId:message.threadId,receivedAt:evidence.receivedAt},evidence:'Authenticated Canva cancellation confirmation received '+new Date(evidence.receivedAt).toISOString()+'.',nextAction:undefined};
       await replace(u,t.id,confirmed);
       const pending=await listAllJobs(j=>j.userId===u&&j.trialId===t.id&&j.status==='PENDING');
       if(pending.length)await db.delete('jobs',pending.map(j=>j.id));
       await audit(u,t.id,'CANCELED_CONFIRMED','Authenticated Canva confirmation email independently verified the provider result; remaining scheduled actions were revoked.');
       await addNotice(u,{kind:'cancellation_verified',title:'Canva cancellation verified',message:'Trialvisor matched an authenticated Canva confirmation email. No complete email body was retained.',severity:'success',trialId:t.id,provider:'Canva',actionView:'overview'});
       await notifications.send({userIds:[u],notification:{title:'Cancellation verified',body:'Canva cancellation was verified from authenticated provider evidence.'},data:{trialId:t.id,kind:'verified'}});
       verifiedCancellations++;matched++;
      }else if(eligible.length>1){
       await addNotice(u,{kind:'verification_ambiguous',title:'Canva confirmation needs review',message:'One authenticated confirmation could match multiple protected Canva items, so Trialvisor did not mark any cancellation verified.',severity:'warning',provider:'Canva',actionView:'overview'});
      }
      continue;
     }
     const signal=classifySignal(message),sourceRecord={messageId:id,threadId:message.threadId,processedAt:new Date().toISOString(),matched:!!signal,kind:'subscription_signal',processorVersion:GMAIL_PROCESSOR_VERSION};
     if(prior)await db.update(table(u,'gmail-sources'),[{id:prior.id,record:{...prior,...sourceRecord}}]);
     else await db.add(table(u,'gmail-sources'),[sourceRecord]);
     if(!signal)continue;
     matched++;if(signal.confidenceBand==='HIGH')high++;else if(signal.confidenceBand==='MEDIUM')medium++;else low++;
     const related=existingTrials.some(t=>t.sourceRef?.messageId===id||t.sourceRef?.threadId===message.threadId||(t.provider.toLowerCase()===signal.provider.toLowerCase()&&(normalizedPlan(t.plan)===normalizedPlan(signal.plan)||(t.price>0&&signal.price>0&&Math.abs(t.price-signal.price)<.01))&&Math.abs(new Date(t.trialEnd).getTime()-new Date(signal.trialEnd).getTime())<7*86400000));
     if(related){duplicates++;continue}
     const [trialId]=await db.add(table(u,'trials'),[{userId:u,...signal}]);
     if(trialId){existingTrials.push({id:trialId,userId:u,...signal});created++}
    }
   }
   if(ids.size>GMAIL_CANDIDATE_CAP)capped=true;
  const now=new Date().toISOString(),record={userId:u,providerAccountIdentifier:rec.providerAccountIdentifier,historyId:profile.historyId,lastSync:now,processorVersion:GMAIL_PROCESSOR_VERSION};
  if(checkpoint)await db.update(table(u,'google-sync'),[{id:checkpoint.id,record}]);else await db.add(table(u,'google-sync'),[record]);
  const connections=(await db.list<any>(table(u,'connections'),{limit:50})).items.filter(x=>x.provider==='Google'&&x.mode==='PRODUCTION');
  if(connections.length)await db.update(table(u,'connections'),connections.map(x=>({id:x.id,record:{...x,status:'CONNECTED',lastSync:now}})));
 }
 const mode=Array.from(modes).join('+')||'incremental_no_changes';
 await audit(u,'connection','GMAIL_SYNC_COMPLETED','Gmail '+mode+' processed '+scanned+' bounded candidates, matched '+matched+', created '+created+' review-only findings, and skipped '+duplicates+' duplicates.');
 if(created>0)await addNotice(u,{kind:'trial_detected',title:created+' trial or subscription signal'+(created===1?'':'s')+' found',message:'Review each finding before enabling protection. Detection did not authorize cancellation.',severity:'warning',provider:'Google',actionView:'overview'});
 return{ok:true,mode,scanned,matched,created,verifiedCancellations,duplicates,fetchFailures,confidence:{high,medium,low},incrementalCandidates,searchCandidates,capped,processorVersion:GMAIL_PROCESSOR_VERSION};
};
const redirect=(location:string)=>({statusCode:302,headers:{Location:location,'Cache-Control':'no-store'},body:''});
const table=(u:string,n:string)=>n+':'+u;
const audit=async(userId:string,trialId:string,type:string,detail:string)=>{await db.add(table(userId,'audit'),[{userId,trialId,type,detail,at:new Date().toISOString()}])};
const addNotice=async(userId:string,notice:Omit<PersistentNotice,'userId'|'at'|'readAt'>)=>{await db.add(table(userId,'notifications'),[{userId,...notice,at:new Date().toISOString(),readAt:null}])};
const listAllJobs=(predicate:(j:Job&{id:string})=>boolean,limit=500)=>listAllMatching<Job&{id:string}>(db,'jobs',predicate,limit);
const deleteAllRecords=(name:string)=>deleteAllPagedRecords(db,name);
const activityTitle:Record<string,string>={TRIAL_DETECTED:'Trial detected',TRIAL_CREATED:'Trial added for review',AUTHORIZATION_GRANTED:'Protect authorization recorded',DECISION_NOTIFICATION_SENT:'Keep or Cancel decision requested',NOTIFICATION_SENT:'Decision reminder sent',KEEP_SELECTED:'Keep selected',CANCEL_SELECTED:'Cancellation queued',CANCELLATION_STARTED:'Cancellation attempted',ASSIST_REQUIRED:'Action Required raised',PROVIDER_ADAPTER_UNAVAILABLE:'Provider automation unavailable',PROVIDER_STEP_REPORTED:'Provider steps completed',CANCELED_CONFIRMED:'Cancellation verified',CANCELLATION_FAILED:'Cancellation failed',ACCOUNT_CONNECTED:'Account connected',ACCOUNT_DISCONNECTED:'Account disconnected',GMAIL_SYNC_COMPLETED:'Inbox synchronization completed',GOOGLE_REAUTH_REQUIRED:'Provider authentication required'};
const activityActor=(type:string):'USER ACTION'|'TRIALVISOR ACTION'|'PROVIDER RESULT'=>['TRIAL_CREATED','AUTHORIZATION_GRANTED','KEEP_SELECTED','CANCEL_SELECTED','PROVIDER_STEP_REPORTED'].includes(type)?'USER ACTION':['CANCELED_CONFIRMED','CANCELLATION_FAILED','GOOGLE_REAUTH_REQUIRED'].includes(type)?'PROVIDER RESULT':'TRIALVISOR ACTION';
const customerActivity=(a:any)=>({id:a.id,type:a.type,trialId:a.trialId,title:activityTitle[a.type]||'Protection activity',detail:String(a.detail||'Activity recorded.').slice(0,500),at:a.at,actor:activityActor(a.type)});
const ensureAccount=async(u:string)=>{const records=(await db.list<any>(table(u,'account'),{limit:20})).items,now=new Date().toISOString();if(records.length){if(records.every(x=>x.lifecycleVersion===3))return records[0];const corrected=records.map(x=>({...x,lifecycleVersion:3,onboardingComplete:false,onboardingCompletedAt:undefined,guidanceShown:Array.isArray(x.guidanceShown)?x.guidanceShown:[]}));await db.update(table(u,'account'),corrected.map(x=>({id:x.id,record:x})));return corrected[0]}const record={userId:u,lifecycleVersion:3,initializedAt:now,onboardingComplete:false,guidanceShown:[] as string[]};const [id]=await db.add(table(u,'account'),[record]);return{...record,id}};
const integrationDiagnostics=async(u:string)=>{const tokens=(await db.list<GoogleTokenRecord>(table(u,'google-tokens'),{limit:10})).items,active=tokens.filter(x=>!x.revokedAt&&!!x.ciphertext),sync=(await db.list<any>(table(u,'google-sync'),{limit:10})).items,sources=(await db.list<any>(table(u,'gmail-sources'),{limit:500})).items,trials=await listTrials(u),audits=(await db.list<any>(table(u,'audit'),{limit:100})).items;return{tenantUserId:u,google:{connected:active.length>0,accountCount:active.length,encryptedRecords:active.map(x=>({providerAccountIdentifier:x.providerAccountIdentifier,ciphertextPresent:!!x.ciphertext,ivPresent:!!x.iv,tokenExpiration:x.tokenExpiration,authorizedScopes:x.authorizedScopes,updatedAt:x.updatedAt})),callbackValidated:audits.some(x=>x.type==='ACCOUNT_CONNECTED'&&String(x.detail).includes('production OAuth')),refreshObserved:audits.some(x=>x.type==='GOOGLE_TOKEN_REFRESHED'),localDestructionObserved:tokens.some(x=>!!x.revokedAt&&!x.ciphertext&&!x.iv),remoteRevocationAttempted:audits.some(x=>x.type==='GOOGLE_REMOTE_REVOCATION_ATTEMPTED')},gmail:{checkpointCount:sync.length,historyCheckpointPresent:sync.some(x=>!!x.historyId),lastSync:sync.map(x=>x.lastSync).sort().at(-1)||null,processedReferences:sources.length,matchedReferences:sources.filter(x=>x.matched).length,retainedFullBodies:false,confidence:{HIGH:trials.filter(x=>x.confidenceBand==='HIGH').length,MEDIUM:trials.filter(x=>x.confidenceBand==='MEDIUM').length,LOW:trials.filter(x=>x.confidenceBand==='LOW').length},reviewOnly:trials.filter(x=>!!x.sourceRef).every(x=>x.state==='REVIEW_REQUIRED'||(!!x.authorizationAt&&audits.some(a=>a.trialId===x.id&&a.type==='AUTHORIZATION_GRANTED')))}}};
const config=async()=>{const n=await secrets.listSecretNames();return{google:n.includes('GOOGLE_CLIENT_ID')&&n.includes('GOOGLE_CLIENT_SECRET')&&n.includes('GOOGLE_TOKEN_ENCRYPTION_KEY'),microsoft:n.includes('MICROSOFT_CLIENT_ID')&&n.includes('MICROSOFT_CLIENT_SECRET'),stripe:await billingConfigured(),email:n.includes('RESEND_API_KEY')}};
const listTrials=async(u:string)=>(await db.list<Trial>(table(u,'trials'),{limit:50})).items.map(t=>({...t,...providerSupport(t.provider)}));
const findTrial=async(u:string,id:string)=>{const [t]=await db.get<Trial>(table(u,'trials'),[id]);return t?{...t,id}:null};
const replace=async(u:string,id:string,t:Trial)=>{const [ok]=await db.update(table(u,'trials'),[{id,record:t}]);if(!ok)throw new Error('update failed')};
const execute=async(userId:string,id:string)=>{const t=await findTrial(userId,id);if(!t||!['AUTO_CANCEL_ENABLED','DECISION_PENDING'].includes(t.state))return {ok:false,reason:'not_authorized'};await replace(userId,id,{...t,state:'CANCELLATION_RUNNING'});await audit(userId,id,'CANCELLATION_STARTED','Explicit authorization revalidated; no provider action was represented as complete.');const support=t.support||providerSupport(t.provider).support,guided=support==='GUIDED',next={...t,...providerSupport(t.provider),state:'CANCELLATION_NEEDS_USER' as State,nextAction:guided?'Open Canva’s official cancellation guide, complete the cancellation in the correct purchase channel, then return here. Trialvisor will require authenticated provider evidence before marking it verified.':'No reliable Trialvisor cancellation workflow currently exists for this provider. Cancel directly with the provider and retain its confirmation; Trialvisor will not claim a verified outcome.'};await replace(userId,id,next);await audit(userId,id,guided?'ASSIST_REQUIRED':'PROVIDER_ADAPTER_UNAVAILABLE',guided?'Guided Canva workflow requires the customer to act; provider evidence is required for verification.':'No reliable production cancellation workflow is enabled for this provider.');await addNotice(userId,{kind:'action_required',title:'Action required for '+t.provider,message:next.nextAction||'User participation is required before cancellation can continue.',severity:'critical',trialId:id,provider:t.provider,actionView:'overview'});await notifications.send({userIds:[userId],notification:{title:'Action required',body:guided?'Complete Canva cancellation using its official flow, then sync Gmail for provider evidence.':t.provider+' has no reliable Trialvisor cancellation workflow yet.'},data:{trialId:id,kind:'guided'}});return{ok:true,state:next.state}};
export const durableActionsWorker=async()=>{const now=new Date().toISOString();const pendingJobs=await listAllJobs(x=>x.status==='PENDING'&&x.dueAt<=now,20);for(const j of pendingJobs){try{if(j.kind==='DECISION'){const t=await findTrial(j.userId,j.trialId);if(t&&t.state==='AUTO_CANCEL_ENABLED'){await replace(j.userId,j.trialId,{...t,state:'DECISION_PENDING'});await addNotice(j.userId,{kind:'decision_needed',title:'Keep or Cancel decision needed',message:t.provider+' is approaching its billing date. Choose Keep Service or leave the authorized cancellation scheduled.',severity:'warning',trialId:j.trialId,provider:t.provider,actionView:'overview'});await notifications.send({userIds:[j.userId],notification:{title:t.provider+' trial is ending',body:'Keep it or Trialvisor will cancel as previously authorized.'},data:{trialId:j.trialId,kind:'decision'}});await audit(j.userId,j.trialId,'NOTIFICATION_SENT','Decision notification sent.')}}else await execute(j.userId,j.trialId);await db.delete('jobs',[j.id])}catch(e){console.error('durable job failed',j.id);if(j.attempts===0){await addNotice(j.userId,{kind:'cancellation_failed',title:'Scheduled protection action failed',message:'Trialvisor could not complete a scheduled action. Review this item before its billing deadline.',severity:'critical',trialId:j.trialId,actionView:'overview'});await audit(j.userId,j.trialId,'CANCELLATION_FAILED','Scheduled protection action failed and remains unresolved.')}if(j.attempts>=3)await db.delete('jobs',[j.id]);else await db.update('jobs',[{id:j.id,record:{...j,attempts:j.attempts+1}}])}}return{statusCode:200}};
const cleanupSessions=(u:string)=>cleanupSessionsRecords(db,u);
const resolveSession=(u:string,sid?:string)=>resolveSessionRecord(db,u,sid);
const enforceSession=(opts:{touchActivity?:boolean;sensitive?:boolean}={})=>{
  return async(c:RouterContext)=>{
    const sid=getSessionId(c);
    const res=await evaluateSession(db,c.user?.userId,sid,opts);
    if(!res.ok)return error(res.code,res.status);
    return undefined;
  };
};
export const handler=router({
 'GET /api/_healthcheck':[async()=>json({message:'Success'})],
 'POST /api/billing/webhook':[async c=>handleStripeWebhook(c.event)],
 'POST /api/auth/session/init':[requireAuth(),async c=>{
    const u=c.user!.userId,now=new Date().toISOString();
    await cleanupSessions(u);
    const [id]=await db.add(table(u,'sessions'),[{userId:u,createdAt:now,lastActiveAt:now}]);
    return json({ok:true,sessionId:id,createdAt:now,lastActiveAt:now,idleTimeoutMs:SESSION_IDLE_TIMEOUT_MS,absoluteLifetimeMs:SESSION_ABSOLUTE_LIFETIME_MS});
  }],
 'POST /api/auth/session/heartbeat':[requireAuth(),enforceSession({touchActivity:true}),async c=>json({ok:true,lastActiveAt:new Date().toISOString()})],
 'POST /api/auth/session/invalidate':[requireAuth(),async c=>{
     const u=c.user!.userId,now=new Date().toISOString();
     const sid=getSessionId(c,{allowBody:true});
     const b=(c.body||{}) as {everywhere?:boolean};
     const res=await invalidateSessionRecord(db,u,sid,b.everywhere===true,now);
     if(!res.ok){
       return error(res.error||'session_id_required',400);
     }
     return json({ok:true});
   }],
 'GET /api/billing/status':[requireAuth(),enforceSession(),async c=>json(await billingSnapshot(c.user!.userId))],
 'POST /api/billing/checkout':[requireAuth(),enforceSession({touchActivity:true}),async c=>createCheckout(c.user!.userId,c.user!.email,(c.body as {cadence?:unknown}|undefined)?.cadence)],
 'POST /api/billing/portal':[requireAuth(),enforceSession({touchActivity:true}),async c=>createPortal(c.user!.userId)],
 'GET /api/oauth/google/config':[async()=>json({configured:await googleReady(),redirectUri:GOOGLE_REDIRECT_URI,scopes:GOOGLE_SCOPES})],
 'POST /api/oauth/google/start':[requireAuth(),enforceSession({touchActivity:true}),async c=>{if(!await googleReady())return error('Google integration is not configured',503);const nonce=randomHex(32),nonceHash=await sha256(nonce),now=Date.now();const [stateId]=await db.add('oauth-states',[{userId:c.user!.userId,provider:'google',nonceHash,createdAt:new Date(now).toISOString(),expiresAt:new Date(now+10*60*1000).toISOString()}]);if(!stateId)return error('Unable to create OAuth state',500);const clientId=await secrets.readSecret('GOOGLE_CLIENT_ID');const state=stateId+'.'+nonce;const q=new URLSearchParams({client_id:clientId,redirect_uri:GOOGLE_REDIRECT_URI,response_type:'code',scope:GOOGLE_SCOPES.join(' '),access_type:'offline',include_granted_scopes:'true',prompt:'consent',state});return json({authorizationUrl:'https://accounts.google.com/o/oauth2/v2/auth?'+q.toString(),expiresIn:600})}],
 'GET /api/oauth/google/callback':[async c=>{const code=c.query.code,state=c.query.state,oauthError=c.query.error;if(oauthError)return redirect('https://trialvisor-m8vrsu.v2.appdeploy.ai/?oauth=google_denied');if(!code||!state||!state.includes('.'))return error('Invalid OAuth callback',400);const split=state.indexOf('.'),stateId=state.slice(0,split),nonce=state.slice(split+1);const [stored]=await db.get<OAuthState>('oauth-states',[stateId]);if(!stored||stored.provider!=='google'||stored.usedAt||stored.expiresAt<=new Date().toISOString()||stored.nonceHash!==await sha256(nonce))return error('OAuth state is invalid or expired',400);const [claimed]=await db.delete('oauth-states',[stateId]);if(!claimed)return error('OAuth state was already consumed',409);try{const clientId=await secrets.readSecret('GOOGLE_CLIENT_ID'),clientSecret=await secrets.readSecret('GOOGLE_CLIENT_SECRET');const tokenResponse=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code,client_id:clientId,client_secret:clientSecret,redirect_uri:GOOGLE_REDIRECT_URI,grant_type:'authorization_code'})});if(!tokenResponse.ok)throw new Error('Google token exchange failed '+tokenResponse.status);const tokens=await tokenResponse.json() as {access_token:string;refresh_token?:string;expires_in:number;scope?:string;token_type:string;id_token?:string};const profileResponse=await fetch('https://openidconnect.googleapis.com/v1/userinfo',{headers:{Authorization:'Bearer '+tokens.access_token}});if(!profileResponse.ok)throw new Error('Google profile lookup failed '+profileResponse.status);const profile=await profileResponse.json() as {sub:string;email?:string};const encrypted=await encryptTokens(tokens),now=new Date().toISOString();const existing=(await db.list<GoogleTokenRecord>(table(stored.userId,'google-tokens'),{limit:10})).items.find(x=>x.providerAccountIdentifier===profile.sub&&!x.revokedAt);if(existing)await db.update(table(stored.userId,'google-tokens'),[{id:existing.id,record:{...existing,...encrypted,tokenExpiration:new Date(Date.now()+tokens.expires_in*1000).toISOString(),authorizedScopes:tokens.scope||GOOGLE_SCOPES.join(' '),updatedAt:now}}]);else await db.add(table(stored.userId,'google-tokens'),[{userId:stored.userId,provider:'google',providerAccountIdentifier:profile.sub,...encrypted,tokenExpiration:new Date(Date.now()+tokens.expires_in*1000).toISOString(),authorizedScopes:tokens.scope||GOOGLE_SCOPES.join(' '),createdAt:now,updatedAt:now}]);const existingConns=(await db.list<any>(table(stored.userId,'connections'),{limit:50})).items.filter(x=>x.provider==='Google'&&x.mode==='PRODUCTION');const connRecord={provider:'Google',providerAccountIdentifier:profile.email||profile.sub,status:'CONNECTED',lastSync:now,mode:'PRODUCTION'};if(existingConns.length){await db.update(table(stored.userId,'connections'),[{id:existingConns[0].id,record:{...existingConns[0],...connRecord}}]);if(existingConns.length>1)await db.delete(table(stored.userId,'connections'),existingConns.slice(1).map(x=>x.id))}else await db.add(table(stored.userId,'connections'),[connRecord]);await audit(stored.userId,'connection','ACCOUNT_CONNECTED','Google account connected using production OAuth.');return redirect('https://trialvisor-m8vrsu.v2.appdeploy.ai/?oauth=google_connected')}catch(e){console.error('Google OAuth callback failed');await audit(stored.userId,'connection','OAUTH_FAILED','Google OAuth callback failed after state validation.');return redirect('https://trialvisor-m8vrsu.v2.appdeploy.ai/?oauth=google_failed')}}],
 'POST /api/gmail/sync':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId;const syncs=(await db.list<any>(table(u,'google-sync'),{limit:10})).items;const lastSync=syncs.map(x=>new Date(x.lastSync||0).getTime()).sort((a,b)=>b-a)[0];if(lastSync&&Date.now()-lastSync<30000){const waitSec=Math.ceil((30000-(Date.now()-lastSync))/1000);return error('Please wait '+waitSec+'s before syncing Gmail again',429)}try{return json(await syncGoogle(u))}catch(e){const u=c.user!.userId,message=e instanceof Error?e.message:'gmail_sync_failed',reconnect=message==='reconnect_required'||message==='google_not_connected';await addNotice(u,{kind:reconnect?'provider_auth_required':'sync_failure',title:reconnect?'Google reconnection required':'Gmail synchronization failed',message:reconnect?'Reconnect Google before the next inbox synchronization.':'Trialvisor could not complete the inbox synchronization. Existing findings and protection authority were not changed.',severity:reconnect?'critical':'warning',provider:'Google',actionView:'accounts'});return error(message,reconnect?409:503)}}],
 'POST /api/oauth/google/disconnect':[requireAuth(),enforceSession({touchActivity:true,sensitive:true}),async c=>{const u=c.user!.userId,tokens=(await db.list<GoogleTokenRecord>(table(u,'google-tokens'),{limit:10})).items.filter(x=>!x.revokedAt),now=new Date().toISOString();for(const x of tokens){try{const plain=await decryptTokens(x.ciphertext,x.iv),token=plain.refresh_token||plain.access_token;if(token){await audit(u,'connection','GOOGLE_REMOTE_REVOCATION_ATTEMPTED','Google token revocation request initiated.');await fetch('https://oauth2.googleapis.com/revoke',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({token})})}}catch(e){console.warn('Google remote revocation failed; local token will still be destroyed')}}if(tokens.length)await db.update(table(u,'google-tokens'),tokens.map(x=>({id:x.id,record:{...x,revokedAt:now,updatedAt:now,ciphertext:'',iv:''}})));const conns=(await db.list<any>(table(u,'connections'),{limit:50})).items.filter(x=>x.provider==='Google'&&x.mode==='PRODUCTION');if(conns.length)await db.update(table(u,'connections'),conns.map(x=>({id:x.id,record:{...x,status:'DISCONNECTED',lastSync:x.lastSync||now}})));await audit(u,'connection','ACCOUNT_DISCONNECTED','Google token material deleted and connection revoked locally.');await addNotice(u,{kind:'account_disconnected',title:'Google disconnected',message:'Local encrypted OAuth token material was destroyed. Existing Trialvisor findings and audit records were retained.',severity:'info',provider:'Google',actionView:'accounts'});return json({ok:true,reauthorizeWith:'/api/oauth/google/start'})}],
 'GET /api/integrations/google/diagnostics':[requireAuth(),enforceSession(),async c=>json(await integrationDiagnostics(c.user!.userId))],
 'POST /api/account/onboarding-complete':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId;await ensureAccount(u);const records=(await db.list<any>(table(u,'account'),{limit:20})).items,now=new Date().toISOString();await db.update(table(u,'account'),records.map(a=>({id:a.id,record:{...a,lifecycleVersion:3,onboardingComplete:true,onboardingCompletedAt:now}})));return json({ok:true})}],
 'POST /api/account/onboarding-restart':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId;await ensureAccount(u);const records=(await db.list<any>(table(u,'account'),{limit:20})).items;await db.update(table(u,'account'),records.map(a=>({id:a.id,record:{...a,lifecycleVersion:3,onboardingComplete:false,onboardingCompletedAt:undefined}})));return json({ok:true})}],
 'GET /api/dashboard':[requireAuth(),enforceSession(),async c=>{const u=c.user!.userId,account=await ensureAccount(u),trials=await listTrials(u),audits=(await db.list<any>(table(u,'audit'),{limit:100})).items,connections=(await db.list(table(u,'connections'),{limit:50})).items,sources=(await db.list<any>(table(u,'gmail-sources'),{limit:500})).items,sync=(await db.list<any>(table(u,'google-sync'),{limit:10})).items,billing=await billingSnapshot(u),matched=sources.filter(x=>x.matched).length,notices=(await db.list<PersistentNotice>(table(u,'notifications'),{limit:100})).items.sort((a,b)=>b.at.localeCompare(a.at)),activity=audits.sort((a,b)=>b.at.localeCompare(a.at)).map(customerActivity),trialsWithActivity=trials.map(t=>({...t,activity:activity.filter(a=>a.trialId===t.id)}));return json({user:{name:c.user!.name,email:c.user!.email},account:{initialized:true,onboardingComplete:!!account.onboardingComplete,guidanceShown:Array.isArray(account.guidanceShown)?account.guidanceShown:[]},trials:trialsWithActivity,saved:trials.filter(t=>t.state==='CANCELED_CONFIRMED').reduce((s,t)=>s+t.price,0),events:audits.length,activity,notifications:notices,connections,billing,config:await config(),gmailDiscovery:{processedReferences:sources.length,matchedReferences:matched,lastSync:sync.map(x=>x.lastSync).filter(Boolean).sort().at(-1)||null,verified:trials.some(t=>!!t.sourceRef&&sources.some(s=>s.matched&&s.messageId===t.sourceRef?.messageId))}})}],
 'POST /api/guidance/:key/seen':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,key=c.params.key,allowed=new Set(['FIRST_AUTHENTICATED_VISIT','FIRST_ACCOUNT_CONNECTED','FIRST_SUCCESSFUL_INBOX_SYNC','FIRST_TRIAL_DETECTED','FIRST_PROTECT_AUTHORIZATION','FIRST_VERIFIED_CANCELLATION']);if(!allowed.has(key))return error('Unknown guidance key',400);await ensureAccount(u);const records=(await db.list<any>(table(u,'account'),{limit:20})).items;await db.update(table(u,'account'),records.map(a=>({id:a.id,record:{...a,guidanceShown:Array.from(new Set([...(Array.isArray(a.guidanceShown)?a.guidanceShown:[]),key]))}})));return json({ok:true})}],
 'POST /api/trials/manual':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,b=(c.body||{}) as {provider?:string;plan?:string;price?:number;trialEnd?:string},provider=(b.provider||'').trim(),end=new Date(b.trialEnd||'');if(!provider||Number.isNaN(end.getTime()))return error('Provider and valid trial end are required',400);if(end.getTime()<=Date.now())return error('Trial end date must be in the future',400);const deadline=new Date(end.getTime()-48*3600000),run=new Date(deadline.getTime()-3600000),[id]=await db.add(table(u,'trials'),[{userId:u,provider,plan:(b.plan||'Trial').trim()||'Trial',price:Math.max(0,Number(b.price)||0),currency:'USD',trialEnd:end.toISOString(),safeDeadline:deadline.toISOString(),plannedExecution:run.toISOString(),state:'REVIEW_REQUIRED',...providerSupport(provider),confidence:1,confidenceBand:'HIGH'}]);if(!id)return error('Unable to create trial',500);await audit(u,id,'TRIAL_CREATED','Manual trial added for review.');await addNotice(u,{kind:'review_needed',title:provider+' needs review',message:'A manual trial was added. Nothing is protected until you explicitly choose Protect.',severity:'warning',trialId:id,provider,actionView:'overview'});return json({ok:true,id})}],
 'POST /api/notifications/read-all':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,records=(await db.list<PersistentNotice>(table(u,'notifications'),{limit:100})).items.filter(x=>!x.readAt),readAt=new Date().toISOString();if(records.length)await db.update(table(u,'notifications'),records.map(x=>({id:x.id,record:{...x,readAt}})));return json({ok:true,updated:records.length})}],
 'POST /api/notifications/:id/read':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,[record]=await db.get<PersistentNotice>(table(u,'notifications'),[c.params.id]);if(!record)return error('Notification not found',404);await db.update(table(u,'notifications'),[{id:c.params.id,record:{...record,readAt:record.readAt||new Date().toISOString()}}]);return json({ok:true})}],
 'POST /api/feedback':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,b=(c.body||{}) as {category?:string;provider?:string;objectId?:string;screen?:string;detail?:string},allowed=new Set(['incorrect_detection','wrong_date','wrong_amount','duplicate','missing','instructions','status','provider_support','general']),detail=(b.detail||'').trim();if(!allowed.has(b.category||'')||detail.length<10||detail.length>2000)return error('Valid feedback category and details are required',400);if(/(?:ya29\.|bearer\s+|sk_(?:live|test)_|refresh_token|client_secret)/i.test(detail))return error('Remove credential or token material before submitting',400);const [id]=await db.add(table(u,'feedback'),[{userId:u,category:b.category,provider:(b.provider||'').trim().slice(0,100),objectId:(b.objectId||'').trim().slice(0,100),screen:(b.screen||'unknown').trim().slice(0,50),detail,createdAt:new Date().toISOString(),status:'NEW'}]);if(!id)return error('Unable to save feedback',500);return json({ok:true,reference:id.slice(0,8)})}],
 'POST /api/security/report':[async c=>{const b=(c.body||{}) as {category?:string;summary?:string;replyTo?:string;website?:string},allowed=new Set(['account_compromise','oauth','data_exposure','tenant_isolation','authorization','vulnerability']),summary=(b.summary||'').trim(),replyTo=(b.replyTo||'').trim();if(b.website)return json({ok:true,reference:'received'});if(!allowed.has(b.category||'')||summary.length<20||summary.length>3000)return error('Valid category and report details are required',400);if(replyTo&&(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(replyTo)||replyTo.length>200))return error('Reply email is invalid',400);if(/(?:ya29\.|bearer\s+|sk_(?:live|test)_|refresh_token|client_secret)/i.test(summary))return error('Remove credential or token material before submitting',400);const [id]=await db.add('security-reports',[{category:b.category,summary,replyTo:replyTo||undefined,createdAt:new Date().toISOString(),status:'NEW'}]);if(!id)return error('Unable to accept security report',500);return json({ok:true,reference:id.slice(0,8)})}],
 'GET /api/account/export':[requireAuth(),enforceSession(),async c=>{const u=c.user!.userId,names=['account','trials','audit','connections','gmail-sources','google-sync','notifications','feedback','billing'],pages=await Promise.all(names.map(n=>db.list(table(u,n),{limit:n==='gmail-sources'?500:100})));return json({exportedAt:new Date().toISOString(),scope:'Tenant-scoped Trialvisor application records. OAuth token material and secrets are excluded.',data:Object.fromEntries(names.map((n,i)=>[n,pages[i].items]))})}],
 'POST /api/account/delete':[requireAuth(),enforceSession({touchActivity:true,sensitive:true}),async c=>{const u=c.user!.userId,b=(c.body||{}) as {confirm?:string};if(b.confirm!=='DELETE')return error('Explicit deletion confirmation is required',400);const billing=await currentBilling(u);if(billing&&billing.subscriptionId&&ACTIVE_STATUSES.has(billing.status)&&!billing.cancelAtPeriodEnd)return error('Active Stripe subscription must be canceled in the billing portal before deleting your account',409);const bindingTable=billing?.customerId?'stripe-customer-binding:'+billing.customerId:null;if(bindingTable){const bindings=(await db.list<any>(bindingTable,{limit:10})).items;if(bindings.some(x=>x.userId!==u))return error('Stripe customer ownership could not be verified for deletion',409)}const tokens=(await db.list<GoogleTokenRecord>(table(u,'google-tokens'),{limit:10})).items.filter(x=>!x.revokedAt&&x.ciphertext);for(const x of tokens){try{const plain=await decryptTokens(x.ciphertext,x.iv),token=plain.refresh_token||plain.access_token;if(token)await fetch('https://oauth2.googleapis.com/revoke',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({token})})}catch{console.warn('Provider revocation failed during account deletion; local data deletion continued')}}const jobs=await listAllJobs(x=>x.userId===u);if(jobs.length)await db.delete('jobs',jobs.map(x=>x.id));for(const n of ['sessions','google-tokens','google-sync','gmail-sources','connections','trials','audit','notifications','feedback','billing','account'])await deleteAllRecords(table(u,n));if(bindingTable)await deleteAllRecords(bindingTable);return json({ok:true,deleted:'trialvisor_tenant_data',authenticationIdentityDeleted:false})}],
 'POST /api/connections/demo':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,b=(c.body||{}) as {provider?:string};if(!['Google','Microsoft'].includes(b.provider||''))return error('Unsupported provider',400);const now=new Date(),end=new Date(now.getTime()+6*86400000),deadline=new Date(end.getTime()-48*3600000),run=new Date(deadline.getTime()-3600000);const existing=(await db.list<Trial>(table(u,'trials'),{limit:50})).items.find(t=>t.provider===(b.provider==='Google'?'Canva':'Notion'));if(!existing){const provider=b.provider==='Google'?'Canva':'Notion';const [id]=await db.add(table(u,'trials'),[{userId:u,provider,plan:'Pro trial',price:provider==='Canva'?14.99:12,currency:'USD',trialEnd:end.toISOString(),safeDeadline:deadline.toISOString(),plannedExecution:run.toISOString(),state:'REVIEW_REQUIRED',...providerSupport(provider),confidence:.94}]);if(id)await audit(u,id,'TRIAL_DETECTED','Demo inbox signal classified with high confidence.')}await db.add(table(u,'connections'),[{provider:b.provider,status:'CONNECTED',lastSync:new Date().toISOString(),mode:'DEMO'}]);await audit(u,'connection','ACCOUNT_CONNECTED',b.provider+' demo inbox connected.');return json({ok:true})}],
 'POST /api/trials/:id/protect':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId;if(!await hasProEntitlement(u))return error('Trialvisor Pro is required to protect a new trial',402);const t=await findTrial(u,c.params.id);if(!t||t.state!=='REVIEW_REQUIRED')return error('Trial is not eligible',409);const jobs=await listAllJobs(j=>j.userId===u&&j.trialId===c.params.id&&j.status==='PENDING');if(jobs.length)return error('Protection is already scheduled',409);const authorizedAt=new Date().toISOString(),next={...t,state:'AUTO_CANCEL_ENABLED' as State,authorizationAt:authorizedAt};await replace(u,c.params.id,next);await db.add('jobs',[{userId:u,trialId:c.params.id,kind:'DECISION',dueAt:new Date(new Date(t.plannedExecution).getTime()-24*3600000).toISOString(),status:'PENDING',attempts:0,idempotencyKey:'decision:'+c.params.id},{userId:u,trialId:c.params.id,kind:'CANCEL',dueAt:t.plannedExecution,status:'PENDING',attempts:0,idempotencyKey:'cancel:'+c.params.id}]);await audit(u,c.params.id,'AUTHORIZATION_GRANTED','Auto Cancel explicitly authorized for this subscription.');await addNotice(u,{kind:'protection_enabled',title:'Protection enabled for '+t.provider,message:'Trialvisor will monitor the Safe Cancel Deadline. Cancellation authority applies only to this protected item.',severity:'success',trialId:c.params.id,provider:t.provider,actionView:'overview'});return json({ok:true})}],
 'POST /api/trials/:id/keep':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,t=await findTrial(u,c.params.id);if(!t||!['AUTO_CANCEL_ENABLED','DECISION_PENDING','CANCELLATION_NEEDS_USER'].includes(t.state))return error('No revocable cancellation',409);await replace(u,c.params.id,{...t,state:'KEEP_REQUESTED',nextAction:undefined});const jobs=await listAllJobs(j=>j.userId===u&&j.trialId===c.params.id&&j.status==='PENDING');if(jobs.length)await db.delete('jobs',jobs.map(j=>j.id));await audit(u,c.params.id,'KEEP_SELECTED','Scheduled cancellation revoked.');await addNotice(u,{kind:'keep_selected',title:t.provider+' will be kept',message:'The scheduled Trialvisor cancellation was revoked and the decision was recorded.',severity:'info',trialId:c.params.id,provider:t.provider,actionView:'overview'});return json({ok:true})}],
 'POST /api/trials/:id/cancel-selected':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,t=await findTrial(u,c.params.id);if(!t||!['AUTO_CANCEL_ENABLED','DECISION_PENDING'].includes(t.state))return error('No active protection authorization',409);if(t.state==='AUTO_CANCEL_ENABLED')await replace(u,c.params.id,{...t,state:'DECISION_PENDING'});await audit(u,c.params.id,'CANCEL_SELECTED','User confirmed cancellation should proceed under the existing authorization and schedule.');await addNotice(u,{kind:'cancellation_scheduled',title:'Cancellation remains scheduled',message:t.provider+' will proceed at the planned time under your existing authorization unless you choose Keep.',severity:'warning',trialId:c.params.id,provider:t.provider,actionView:'overview'});return json({ok:true})}],
 'POST /api/trials/:id/provider-step-complete':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId,t=await findTrial(u,c.params.id);if(!t||t.state!=='CANCELLATION_NEEDS_USER')return error('Guided provider step is not available',409);const isCanva=/\bcanva\b/i.test(t.provider);const at=new Date().toISOString(),next={...t,...providerSupport(t.provider),providerActionAt:at,nextAction:isCanva?'Waiting for an authenticated Canva cancellation confirmation. Sync the connected Gmail inbox; a self-report alone never marks cancellation verified.':'Customer reported completing cancellation directly with provider. Trialvisor does not mark this verified without independent provider evidence.'};await replace(u,c.params.id,next);await audit(u,c.params.id,'PROVIDER_STEP_REPORTED',isCanva?'Customer reported completing the provider steps; cancellation remains unverified pending authenticated provider evidence.':'Customer reported completing provider cancellation directly. Outcome remains unverified.');await addNotice(u,{kind:'verification_pending',title:t.provider+' steps reported',message:isCanva?'Sync the connected Gmail inbox after the provider confirmation arrives. Trialvisor will not treat this report as proof.':'Cancellation was reported by user. Trialvisor will not claim a verified outcome without independent provider evidence.',severity:'warning',trialId:c.params.id,provider:t.provider,actionView:isCanva?'accounts':'overview'});return json({ok:true,verified:false})}],
 'POST /api/trials/:id/execute':[requireAuth(),enforceSession({touchActivity:true}),async c=>json(await execute(c.user!.userId,c.params.id))],
 'POST /api/demo/reset':[requireAuth(),enforceSession({touchActivity:true}),async c=>{const u=c.user!.userId;for(const n of ['trials','audit','connections']){const x=(await db.list(table(u,n),{limit:100})).items;if(x.length)await db.delete(table(u,n),x.map(i=>i.id))}const jobs=await listAllJobs(j=>j.userId===u);if(jobs.length)await db.delete('jobs',jobs.map(j=>j.id));return json({ok:true})}]
});
